<%= addendum_storage 'photo' %>

<%= form_for Photo.add_billet, url: photos_path, html: { multipart: true, class: 'form-horizontal' } do |form| %>

  <%= render 'shared/selects', f: form %>

  <%= form.fields_for :picture do |fields| %>

    <div class='form-group'>
      <%= fields.label :image, class: 'col-sm-2 control-label' %>
      <div class='col-sm-10'>
        <%= fields.file_field :image, class: 'form-control' %>
      </div>
    </div>

  <% end %>

  <div class='form-group'>
    <%= form.label :comment, class: 'col-sm-2 control-label' %>
    <div class='col-sm-10'>
      <%= form.text_area :comment, class: 'form-control' %>
    </div>
  </div>

  <div class='form-group'>
    <div class='col-sm-offset-2 col-sm-10'>
      <%= form.submit class: 'btn btn-primary', id:'submit-photo', style:'display:none;' %>
    </div>
  </div>

<% end %>

<% content_for :action do %>
  <%= link_to "Upload", '#', class:'btn upload-button', data:{submit_button:true, submit_target:'#submit-photo'} %>
<% end %>
